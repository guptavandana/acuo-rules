package demo.eligibleassetrule;

import java.lang.Number;

rule "rule-eea-FXHaircut-vm-a"
	dialect "mvel"
	when
		as : Asset( type != "cash" )
		ag : Agreement( marginType == "Vriation" , eligibleCurrency excludes as.currency )
	then
		as.setFXHaircut( 0.08 );
end
