package com.acuo.rules.marginrules

import org.slf4j.Logger

global Logger log;

rule "margin rule hi"
    when
        LocalAsset(type == "cash");
    then
        log.info("Rule: '{}'",   drools.getRule().getName() );
end
