package demo.eligibleassetrule;

import java.lang.Number;

rule "rule-eea-eli-j" extends "rule-eea-class-j"
	dialect "mvel"
	when
		asset : Asset( CQS in ( 1, 2, 3 ) )
	then
		asset.setEligible( true );
end
